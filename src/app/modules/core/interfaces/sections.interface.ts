import { Tasks } from './tasks.interface';

export interface Sections {
  job?: Tasks[];
  html?: Tasks[];
  css?: Tasks[];
  bootstrap?: Tasks[];
  javascript?: Tasks[];
  typescript?: Tasks[];
  angular?: Tasks[];
  gitBasics?: Tasks[];
  gitExpert?: Tasks[];
  nestJS?: Tasks[];
}
