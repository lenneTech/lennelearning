import { NbMenuItem } from '@nebular/theme';
import { SectionService } from '../../../modules/core/services/section.service';

export class Section {
  constructor(protected readonly sectionService: SectionService) {}
}
